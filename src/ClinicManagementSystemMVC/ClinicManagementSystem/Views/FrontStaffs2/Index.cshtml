@model IEnumerable<ClinicManagementSystem.Models.FrontStaff>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StaffUsername)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Password)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StaffUsername)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Password)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StaffUsername">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StaffUsername">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StaffUsername">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
